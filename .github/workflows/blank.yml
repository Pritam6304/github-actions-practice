name: Metrix jobs
on: 
  push:
    branches:
      - main
jobs: 
  test:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        include:
          - os: ubuntu-latest
    steps:
      - name: Run Test
        run: |
         echo 'Running test on: ${{matrix.os}}'
